{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "^apply_patch$",
        "hooks": [
          {
            "type": "command",
            "command": "sh -c 'root=$(git rev-parse --show-toplevel) && exec node \"$root/scripts/codex/lint-changed.mjs\"'",
            "commandWindows": "powershell -NoProfile -Command \"$root = git rev-parse --show-toplevel; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node (Join-Path $root 'scripts/codex/lint-changed.mjs')\"",
            "timeout": 30,
            "statusMessage": "Linting changed TypeScript files"
          }
        ]
      }
    ]
  }
}
